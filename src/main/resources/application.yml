spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_trade?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: *
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.booktrade.entity
