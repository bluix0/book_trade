<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.booktrade.mapper.UserMapper">

    <!-- 根据用户名查询 -->
    <select id="findByUsername" parameterType="String" resultType="com.booktrade.entity.User">
        SELECT id, username, password, create_time as createTime
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 登录 -->
    <select id="login" parameterType="map" resultType="com.booktrade.entity.User">
        SELECT id, username, password, create_time as createTime
        FROM user
        WHERE username = #{username} AND password = #{password}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.booktrade.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, create_time)
        VALUES (#{username}, #{password}, #{createTime})
    </insert>

    <!-- 查询所有用户 -->
    <select id="findAll" resultType="com.booktrade.entity.User">
        SELECT id, username, password, create_time as createTime
        FROM user
    </select>

</mapper>